# ========================
# DNS Stack Repo .gitignore
# ========================

# --- Pi-hole runtime data ---
dns/pihole/etc-pihole/
dns/pihole/etc-dnsmasq.d/

# --- Nebula-sync backups ---
dns/backups/

# --- Unbound runtime data ---
dns/unbound/root.key
dns/unbound/var/

# (root.hints is intentionally kept — track if you want consistent seeds)

# --- Docker volumes / temp ---
**/var/
**/*.log
**/*.pid

# --- Node / JS caches (future proofing) ---
**/node_modules/

# --- macOS cruft ---
.DS_Store
.AppleDouble
.LSOverride